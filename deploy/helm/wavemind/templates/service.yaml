apiVersion: v1
kind: Service
metadata:
  name: {{ include "wavemind.fullname" . }}
  labels:
    {{- include "wavemind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "wavemind.selectorLabels" . | nindent 4 }}

