global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/prometheus-alerts.yml

scrape_configs:
  - job_name: wavemind
    metrics_path: /metrics
    static_configs:
      - targets: ["wavemind:8000"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

